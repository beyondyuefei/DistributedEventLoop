# DistributedEventLoop
提供一种在分布式集群环境下，如何线程安全的处理业务逻辑的通用框架。它避免了使用并发原语可能带来的性能问题，以及并发使用不当而导致的错误。
- 一级路由：支持分布式环境下根据业务key路由到指定机器节点
- 二级路由：根据业务key分配一个EventLoop，基于EventLoop的线程安全实现相关的业务逻辑 (单线程、不共享，则总是线程安全的，业务代码写起来舒服轻松)
